import { DescShorterPipe } from './desc-shorter.pipe';

describe('DescShorterPipe', () => {
  it('create an instance', () => {
    const pipe = new DescShorterPipe();
    expect(pipe).toBeTruthy();
  });
});
